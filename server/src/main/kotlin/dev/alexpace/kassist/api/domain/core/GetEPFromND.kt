package dev.alexpace.kassist.api.domain.core

import dev.alexpace.kassist.api.domain.models.EmergencyPlan
import dev.alexpace.kassist.api.domain.responses.Feature

/**
 * Returns a list of EmergencyPlans from the retrieved NaturalDisasters
 */
fun GetEPFromND(naturalDisasters: List<Feature>): List<EmergencyPlan> {

    return emptyList()
}